#include <iostream>
#define ll long long
using namespace std;

const int maxn = 1e5 + 10;
int n,a[maxn];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	cin >> n;

	for (int i = 0; i < n; i++) cin >> a[i];

	
}