#include <bits/stdc++.h>
using namespace std;

using i64 = long long;

void solve(int x){
	i64 n,c;
	cin >> n >> c;

	i64 l = 0,r = 1e9;
	while(l <= r){
		i64 mid = (l + r) / 2;
	}
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int t;
	cin >> t;
	for(int i = 1;i <= t;i ++){
		solve(i);
	}

	return 0;
}