#include <bits/stdc++.h>
using namespace std;

using i64 = long long;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int n;
	cin >> n;

	string name;
	map<string,int> gra;
	int x,mx;
	string ans;
	while(n --){
		
	}

	cout << ans << "\n";
}