#include <bits/stdc++.h>
using namespace std;

int t,n,m,sx,sy,ex,ey;
char op[5];

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);

	while(cin >> t){
		while(t --){
			cin >> n >> m >> sx >> sy >> ex >> ey >> op;
			if(op[0] == 'U'){
				if(op[1] == 'L'){
					
				}else if(op[1] == 'R'){
					
				}
			}else if(op[0] == 'D'){
				if(op[1] == 'L'){

				}else if(op[1] == 'R'){
					
				}
			}

		}
	}
	return 0;
}