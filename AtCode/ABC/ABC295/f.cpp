#include <bits/stdc++.h>
using namespace std;

int n,x,ans;
string s,l,r;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	while(cin >> n){
		while(n --){
			cin >> s >> l >> r;
			for(int i = 1;i <= )
		}
	}
	return 0;
}