#include <bits/stdc++.h>
using namespace std;

using i64 = long long;

const int maxn = 1e6 + 10;
int ne[maxn];

void solve()
{
	int n,m;
	cin >> n >> m;
	for(int i = 1;i <= n;i ++)cin >> a[i];
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int t;
	cin >> t;
	while(t --){
		solve();
	}
	return 0;
}